---
export interface Props {
	id: string;
	file?: string;
}

const { id, file } = Astro.props;

let src = `https://gist.github.com/markadrake/${id}.js`;
if(file) {
	src += `?file=${file}`;
}
---

<script src={src}></script>